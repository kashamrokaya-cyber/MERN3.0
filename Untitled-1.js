// 127.0.0.1:3000 we can use insted of localhost:3000,
// [::1]:3000  insted of localhost:3000

// always we can  request  (get) element of http  in browser not post, patch, update

// always we send data in the form of JSON format ({...}); not string like res.send("mern")



//SQL-ORM                    and                            NoSQL-ODM
//sequilize        gives users.find()   ;            mongooset          - package there are many methods like createAt(),find(),delete()
//prisma


// in an app has many methods like listen, get, post and etc.

                           // 127.0.0.1:3000 we can use insted of localhost:3000,
                              // [::1]:3000  insted of localhost:3000

// always we can  request  (get) element of http  in browser not post, patch, update

// always we send data in the form of JSON format ({...}); not string like res.send("mern");
//ORM and ODM 



//we need ORM(is mongooose)to connect the db and server.

//we use mongoose for communicating mongodb and node server,

 // there are two types 
 //cjs common js-> requre ,module.exports
 //ES  EcmaScript-> import ,export default